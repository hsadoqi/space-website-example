import React, { FC } from 'react';

const Hero: FC = () => {
    return (
        <section id="hero">
            <div id="hero-content">

            </div>
        </section>
    );
};

export default Hero;
